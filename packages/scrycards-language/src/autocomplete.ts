import {
    CompletionContext,
    type CompletionSource,
} from "@codemirror/autocomplete";

export const ScrycardsAutocomplete: CompletionSource = (
    context: CompletionContext
) => {
    return null;
};
